name: Daily Play Store Agent Monitor

on:
  schedule:
    # Run daily at 02:00 UTC
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      app_id:
        description: "App Package Name to monitor (e.g., com.example.app)"
        required: false
        default: "com.spotify.music"

permissions:
  contents: read

jobs:
  monitor:
    name: Run Play Store Agent Workflow
    runs-on: ubuntu-latest

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - name: Install Dependencies
        run: |
          uv sync --extra dev

      - name: Run Agent Workflow
        env:
          APP_ID: ${{ github.event.inputs.app_id || 'com.spotify.music' }}
          # Pass play store credentials if configured in repository secrets
          GOOGLE_PLAY_STORE_CREDENTIALS: ${{ secrets.GOOGLE_PLAY_STORE_CREDENTIALS }}
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
        run: |
          uv run python run_agent_workflow.py
